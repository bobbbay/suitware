[package]
name = "suitware-hal"
version = "0.1.0"
edition = "2021"

[dependencies]
sim = { path = "../../dev/suitware-sim", package = "suitware-sim", optional = true }

[features]
default = ["sim"]
prod = []
dev  = ["sim"]